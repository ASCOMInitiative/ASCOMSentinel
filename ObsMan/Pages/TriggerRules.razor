@page "/triggerrules"
@namespace ObsMan

@inject Settings settings;
@inject ObsManLogger logger;
@using LogLevel = ASCOM.Common.Interfaces.LogLevel;

<PageTitle>Safety Monitor Trigger Rules</PageTitle>

<h1>Safety Monitor Trigger Rules</h1>

Trace on: @settings.TraceOn @settings.LogLevel

@code {

    protected override async Task OnInitializedAsync()
    {
    }
}
